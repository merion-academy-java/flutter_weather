import 'package:flutter/material.dart';

class DayDataItemWidget extends StatelessWidget {
  const DayDataItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return DayDataItemWidget();
  }
}
